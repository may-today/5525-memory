# 统计页切换性能优化日志

## 2026-06-20

- 确认切页渲染结构会重新挂载退出页，导致时间轴动画重启或额外创建 WebGL 上下文。
- 确认地球 RAF 在组件卸载后未取消。
- 确认时间轴点亮每 15ms 更新一次 state，每次均协调四整年的 SVG 节点。
- 改为稳定 key 保留退出实例、时间轴批处理更新、完整清理 RAF，并降低高分屏地球渲染成本。
- 在切页状态期间暂停地球 WebGL 更新和外圈 CSS 动画，结束后复用原实例并继续旋转。
- 将切页动画调整为 1 秒，并使用更陡的 `cubic-bezier(0.76, 0, 0.24, 1)` 强化中段速度感。
