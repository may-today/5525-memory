# 最小众歌单改用全巡演总演唱次数

## 完成

- 根据产品反馈，已将冷门排序的 `tourCount` 从「随机曲目出现次数」切换为「全巡演所有五月天歌曲段落的总演唱次数」。候选歌曲仍限制为用户在点歌／安可段落听到的五月天歌曲。
- 保留原有排序、主纸条例外与小纸条 `<= 8` 门槛；固定主歌单、点歌、安可与拆分后的串烧都会贡献全巡演次数。

## 验证

- `git diff --check` 通过。
- 针对改动文件运行的 Biome lint 通过。
- `bun run typecheck` 未能执行完：当前安装目录无法解析已声明的 `html-to-image`（`src/pages/SharePage.tsx`），与本次变更无关。
- 全仓 `bun run lint` 亦因 10 项既有诊断未通过，改动文件没有诊断。
