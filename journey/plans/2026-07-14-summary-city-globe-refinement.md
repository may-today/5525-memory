# 城市地图卡片优化

## 目标

优化 `/summary` 的 `SummaryCardCity`：统一静态页头、保留无定位场景的说明、移除底部详情面板、突出居中的 3D 地球，并补全城市标签与更轻的轨迹线。

## 实施

1. 将标题和统计叙事固定在卡片顶部，不依赖定位或入场动画。
2. 以已去过城市数构造定位与无定位两套文案；无场次时给出回填引导。
3. 移除城市胶片条与详情面板，让地球在剩余空间内居中。
4. 为 cobe 标记补充稳定 ID，并以其 CSS anchor 渲染可见城市标签。
5. 降低弧线宽度，完成静态检查与本地页面核验。
