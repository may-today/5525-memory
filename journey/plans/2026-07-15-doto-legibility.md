# Doto 字体可读性调整

## 目标

盘点 Doto 的使用位置，消除信息性文字的半透明效果，并提高点阵字的默认字重，改善深色背景下的可读性。

## 决策

- `.font-geist`（项目的 Doto 工具类）由 300 提升为 500；所有既有信息性数字随之受益。
- 移除预约倒计时占位、预热页分隔符、黑胶标签及刻字的透明文本色。
- 保留巡演记录的年份幽灵数字低透明度，因为它是 `aria-hidden` 的背景装饰，不承载信息。

## 验证

- 检查所有 Doto 使用点不存在信息性 `/<opacity>` 文本色。
- 运行项目的类型检查或构建。
