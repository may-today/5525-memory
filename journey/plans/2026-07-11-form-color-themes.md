# 表单页配色主题调整（2026-07-11）

## 目标

将 `/form` 的通用强调色由品牌橙调整为 `sky-400`，并让已选场次按巡演子主题清晰区分：5525 为粉色、5525+1 为蓝色、5525+2 为橙色。

## 范围

- 在表单根节点作用域覆写 shadcn `primary` 变量，使主要操作按钮跟随蓝色主题。
- 将步骤进度、城市选中数与底栏选中数改为 `sky-400`。
- 由 `subTheme` 显式映射场次行的选中颜色；未知子主题保留数据原有 `themeColor` 作为回退。
- 同步更新设计文档与过程日志。

## 验证

- 运行 lint、typecheck 与 `git diff --check`。
- 核对表单源码中不再遗留橙色的通用强调样式，且三种子主题映射均存在。
