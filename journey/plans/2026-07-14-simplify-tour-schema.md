# 精简巡演数据库结构

## 目标

- 从 `shows` 删除已确认无用的 14 个列。
- 删除不再需要的 `tours`、`tour_types`、`tours_cities` 三张表及其索引。
- 同步收窄服务端查询和前端 `Show` 数据契约，保留当前页面实际使用的数据。

## 实施步骤

1. 已完成盘点现有查询、类型和组件对目标列与三张表的依赖，并确认保留列。
2. 已新增不可变 D1 migration：重建 `shows`、两张引用它的子表，保留数据与报告外键关系，并删除旧表/索引。
3. 已更新数据访问层、相关类型与文档，移除对已删列的读取与可见性过滤。
4. 已在本地 D1 应用迁移，核对 schema、数据行数、外键与关键查询；类型检查与差异格式检查通过。
